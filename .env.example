# Redis Configuration
# For local development (laptop), use localhost
# For production (Beelink/Docker Compose), use 'redis' (service name)

REDIS_HOST=localhost
REDIS_PORT=6379

# Streamlit Configuration
STREAMLIT_SERVER_PORT=8507
STREAMLIT_SERVER_ADDRESS=0.0.0.0
